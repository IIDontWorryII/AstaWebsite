// client/src/pages/admin/NewProtocol.tsx
//
// Thin wrapper around ProtocolForm in "create" mode (no protocol prop).

import ProtocolForm from "./ProtocolForm";

export default function NewProtocol() {
  return <ProtocolForm />;
}
